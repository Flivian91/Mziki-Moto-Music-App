import React from 'react'

function AudioWaveForm() {
  return (
    <div className='flex-grow'>
       {/* <WavesurferPlayer
        height={100}
        waveColor="violet"
        url="/my-server/audio.wav"
        onReady={onReady}
        onPlay={() => setIsPlaying(true)}
        onPause={() => setIsPlaying(false)}
      /> */}
      Wave form goes Heree
    </div>
  )
}

export default AudioWaveForm
